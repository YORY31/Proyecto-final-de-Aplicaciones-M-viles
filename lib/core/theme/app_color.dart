// Importa el paquete principal de Flutter para definir colores.
import 'package:flutter/material.dart';

// Clase que agrupa los colores principales usados en la app.
class AppColors {
  // Color primario (verde) para elementos destacados.
  static const Color primary = Color(0xFF4CAF50);
  // Color secundario (amarillo) para acentos y detalles.
  static const Color secondary = Color(0xFFFFC107);
  // Color de fondo general de la app.
  static const Color background = Color(0xFFF5F5F5);
  // Color principal para textos.
  static const Color textPrimary = Color.fromARGB(255, 33, 33, 33);
  // Color secundario para textos menos importantes.
  static const Color textSecondary = Color(0xFF757575);
}
